# Server-side only (read by api/generate.ts). Never expose these with a VITE_ prefix.
DASHBOARD_PROXY_URL=https://30-apps-dashboard.thermh.in
DASHBOARD_PROXY_SECRET=your-shared-secret-here

# Optional: override the Gemini model requested through the dashboard proxy.
GEMINI_MODEL=gemini-flash-latest
